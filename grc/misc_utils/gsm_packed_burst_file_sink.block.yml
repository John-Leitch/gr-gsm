# auto-generated by grc.converter

id: gsm_packed_burst_file_sink
label: Packed Burst File Sink

parameters:
-   id: filename
    label: Destination file
    dtype: file_open
    default: /tmp/bursts

inputs:
-   domain: message
    id: in

templates:
    imports: import grgsm
    make: grgsm.packed_burst_file_sink(${filename})

file_format: 1
