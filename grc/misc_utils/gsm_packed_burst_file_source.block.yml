# auto-generated by grc.converter

id: gsm_packed_burst_file_source
label: Packed Burst File Source

parameters:
-   id: filename
    label: Source file
    dtype: file_open
    default: /tmp/bursts

outputs:
-   domain: message
    id: out

templates:
    imports: import grgsm
    make: grgsm.packed_burst_file_source(${filename})

file_format: 1
